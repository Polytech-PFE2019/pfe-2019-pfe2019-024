package variable;

public class Compteur extends Variable {
    public Compteur(String nom, String libellé, String description, String annotation) {
        super(nom, libellé, description, annotation);
    }
}
